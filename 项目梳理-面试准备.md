# IGPO 项目梳理（面试准备）

> 按「训练 / 推理 / 整体」三段式梳理，每个环节覆盖常考点。配合 `READING_GUIDE.md`（代码层面）和 `Agentic-RL-面试复习笔记.md`（知识点）食用。

---

## 第一部分：训练环节

### 1.1 模型

**用了什么**：
- 基座：**Qwen3-4B-Thinking-2507**（4B参数，带思维链能力）
- 微调方式：**LoRA**（rank=16, alpha=32, target=all-linear）

**为什么这么选**：
- **预算约束**：只有 2×A800-80GB。原版IGPO用 Qwen2.5-7B 全参微调 + 8卡，显存和算力都不够
- **LoRA 而非全参**：4B全参FSDP在2卡上显存紧张（activations+optimizer states），LoRA只训练少量低秩矩阵，显存降一个量级
- **选 Thinking 变体**：多跳推理需要思维链，Thinking版本原生支持 `<think>` 推理过程
- **4B 而非 7B**：先在 smoke 集验证流程跑通，4B训练快、迭代成本低

**试过别的吗**：早期考虑过 1.7B（更快），但实测推理能力太弱，F1基线过低，放弃了（见 memory_training_params）

**和原版的差异及代价**：
- 模型容量小一半 + LoRA参数效率训练 → 上限低于原版全参7B
- 但足以验证 IGPO 算法本身的增益（Baseline→GRPO→IGPO 的相对提升）

---

### 1.2 数据

**数据怎么来的**：
- 来源：**2WikiMultihopQA**（多跳问答数据集，原始167K条）
- 选 compositional 类型（需要多步推理）→ 24,923条
- 转成 IGPO 统一格式：`data_source / prompt / ability / reward_model / extra_info`

**怎么洗的（BM25可回答性过滤）**：
- **背景**：本地检索（39万Wikipedia passage）覆盖有限，必须保证训练样本的答案能被搜到
- **规则**：用整句问题作BM25 query检索top-10 → 归一化（去标点小写）→ 检查ground_truth是否作为子串出现 → 二元命中/未命中
- 结果：24,923 → **12,685**（通过率50.9%）
- smoke测试再采样1,000条

**有没有额外合成**：
- **没有用外部合成数据**
- **SFT warmup数据是"自蒸馏"**：从RL训练的成功rollout中提取F1=1.0的轨迹（81条），教模型基本工具调用格式

**怎么确保数据有效性**：
- BM25过滤保证答案可检索（否则reward永远0，学不到东西）
- **但暴露了问题**：过滤后80%是director类（假多跳，答案线索在问题里），family类仅25%

**数据失衡的根因（重要）**：
| 类型 | 通过率 | 原因 |
|------|--------|------|
| director | 79.5% | 电影名是稀有词，BM25直接命中 |
| family | 25.2% | 真多跳，需要分跳检索，整句query搜不到 |
| 是/否(comparison) | 0% | 答案是boolean，词法永远搜不到 |

**修复**：分层均衡采样 → 863条（每类~200），解决模型过拟合单一搜索pattern

---

### 1.3 算法

**用了什么**：
- **IGPO**（Information Gain-based Policy Optimization）
- 核心：在GRPO基础上，用 **turn-level信息增益** 作为内在奖励

**info gain 怎么算**：
```
info_gain[turn] = log P(ground_truth | 第turn轮后的上下文) 
                - log P(ground_truth | 第turn轮前的上下文)
```
即：这一轮搜索结果让模型对正确答案的置信度提升了多少。

**reward 结构**：
- 中间每轮的最后一个token ← info_gain（内在奖励，引导搜索策略）
- 最后一轮的最后一个token ← F1（外在奖励，衡量答案对错）
- 格式错误 ← -2.0 惩罚

**advantage 计算**：
- GRPO组内归一化（同一问题的8条rollout）
- F1 和 info_gain **分开归一化**（量级不同，joint会淹没IG信号）
- turn-level累积，**γ=1.0 不折扣**（每轮信息同等重要）

**为什么选 IGPO（核心动机）**：
- **解决信用分配问题（Credit Assignment）**：纯GRPO只用最终F1，模型不知道5轮搜索里哪轮有价值。IGPO给每轮即时反馈
- **引导搜索策略**：模型学到"什么样的query能获取有用信息"，而非只学"怎么答对"

**试过别的吗（消融对照）**：
| 方法 | F1 | 说明 |
|------|-----|------|
| Baseline（不训练）| 21.0 | 纯基座+本地检索 |
| **GRPO**（F1 only, max_turns=1）| 26.4 | 消融：去掉info gain |
| **IGPO**（F1 + info gain）| **34.1** | 完整算法 |

IGPO 比 GRPO +29.2%，证明 turn-level 信号有效。

**算法区别及适用场景**：
- **GRPO**：适合单轮任务（如数学题），只有outcome reward
- **IGPO**：适合**多轮交互/搜索任务**，需要中间步骤反馈
- 本项目是多轮搜索，所以IGPO对症

**对当前场景的理解**：
多跳问答的本质难点不是"会不会答"，而是"会不会搜"——能否把一个多跳问题拆成正确的搜索序列。IGPO的info gain正好奖励"搜到了有用信息"这个中间目标。

---

### 1.4 资源

**硬件**：
- **2× A800-80GB**
- FSDP FULL_SHARD 分布式
- vLLM 做 rollout 生成（gpu_memory_utilization=0.30）

**训练中遇到的问题及解决**：

| 问题 | 现象 | 根因 | 解决 |
|------|------|------|------|
| Qwen3 think标签双重注入 | 31/32 rollout格式错误 | Qwen3-Thinking的chat template已自动加`<think>`，原代码又手动append一个 | 删除手动append（generation.py L436） |
| tool结果序列化崩溃 | chat template报错 | Qwen3要求content是string，原版传list/dict | json.dumps序列化（L746） |
| SFT双重chat template | 训练崩 | 对预格式化prompt又套一次template | 加preformatted检测跳过 |
| 显存OOM | 大batch爆显存 | 4B+FSDP+vLLM同时占显存 | 降batch、gpu_mem_util=0.30、context限6400 |
| 上下文截断 | 多轮搜索超长 | 6400 token限制 | 限制max_turns=5（原版10） |

---

### 1.5 训练慢怎么办 / 如何监控

**训练慢的原因及优化**：
- **rollout自回归生成是大头**（占60-70%时间，逐token顺序生成无法并行）
- info gain 额外增加 20-40%（O(T·L²)前向，但前向比前向+反向便宜）
- 优化手段：vectorized_gt_logprob 批量算gt logprob；vLLM加速生成；LoRA减少反向开销

**监控指标**（tensorboard）：
- **reward曲线**：看是否上升/plateau
- **KL散度**：策略漂移程度（本项目持续上升到50+，是潜在风险）
- **format error rate**：格式错误率（Qwen3适配前31/32，修复后接近0）
- **F1 / info_gain 分别的均值**

---

### 1.6 调优 / Badcase 分析

**遇到的典型badcase**：

**Badcase 1：reward在step 50后plateau甚至下降**
- **分析过程**：不只看指标，抽查模型实际输出 → 发现模型一直在搜"director"相关问题
- **统计**：训练集801/1000是director类
- **根因**：数据失衡导致模型过拟合到单一搜索pattern `搜"X director" → 搜"导演名 属性"`
- **解决**：构造均衡数据集（863条，每类~200）

**Badcase 2：family类问题F1极低**
- **分析**：模型对family问题（"X的父亲国籍"）几乎答不出
- **根因**：①训练数据family类少 ②BM25词法检索搜不到"father→married to"语义关系
- **解决思路**：①均衡采样 ②提出双路并行检索改进（BM25+向量RRF）

**调优具体动作**：
- LR：actor 1e-6（RL要小步），SFT 2e-5（ imitation可以大步）
- max_turns：5（平衡效果和成本）
- n=8 rollouts（GRPO组大小，太少advantage噪声大）
- temperature=1.0（rollout需要探索）

---

## 第二部分：推理环节

### 2.1 输入（Prompt）

**输入是什么**：
- System prompt + 用户问题（多跳问题）
- 工具定义：web_search（参数是query数组）

**System prompt 调优**：
- **原版**（在线搜索）："You are Deep AI Research Assistant" + 通用web搜索指引
- **本项目**（本地BM25）：改成关键词检索指引
  > "Use short, specific keyword queries (2-5 words) in English only. For multi-hop questions, search ONE step at a time."

**为什么不同prompt效果不同**：
- **BM25是词法检索**，喂自然语言长句效果差（停用词稀释、IDF信号弱）
- 喂"短关键词"能让稀有词（人名/电影名）的IDF主导检索
- **本质**：prompt要匹配检索引擎的特性，BM25要关键词，向量检索要自然语言

**效果好的prompt为什么好**：
- 短关键词 → 稀有实体词IDF高 → 精确命中目标passage
- 一次搜一跳 → 避免一个query塞太多信息把BM25搞糊涂
- "搜到就停" → 避免浪费轮次

### 2.2 模型（训练同上 + 部署推理）

**训练**：见第一部分（IGPO RL + SFT warmup）

**部署/推理加速**：
- 用 **vLLM** 做推理（PagedAttention + continuous batching）
- eval时 max_turns=10（比训练的5多，给更多搜索机会）
- temperature=0（贪心，结果可复现）
- 单rollout（n=1，不需要采样多条）

### 2.3 输出

**输出格式**：
```
<think>推理过程</think>
<answer>最终答案</answer>
```
或中间轮：
```
<think>推理</think>
<tool_call>{"name":"web_search","arguments":{"query":["keyword"]}}</tool_call>
```

**怎么保证输出质量**：
- **格式校验**：`check_tags_balance` 检查 `<think>`/`<answer>` 闭合，不合法给-2惩罚
- **答案抽取**：从`<answer>`标签提取，与ground_truth算F1
- **多答案处理**：`<|answer_split|>` 分隔的多个正确答案，取第一个匹配

**输出不行怎么办（兜底）**：
- 格式错误 → 强制结束该rollout，reward=-2（训练时让模型学会规范输出）
- 搜索无结果 → tool返回"No results"，prompt里引导"换关键词重试"
- 答案搜不到（本地索引覆盖不到）→ 这是已知上限，靠双路检索改进

---

## 第三部分：项目整体

### 3.1 STAR 法则

**Situation（背景）**：
- 想复现 ICLR 2026 的 IGPO 论文，学习 RL + Agent + 搜索的结合
- 原版依赖在线搜索API（Google/Bing），有成本和不可复现问题

**Task（任务）**：
- 把 IGPO 改造成**完全离线**的本地检索方案，保证可复现评估
- 在有限算力（2×A800）下验证 IGPO 相对 GRPO 的增益

**Action（行动）**：
1. 搭建本地检索引擎：2WikiMultihopQA的39万Wikipedia passage → tantivy BM25索引 → HTTP服务
2. 数据工程：BM25可回答性过滤 → 发现类型失衡 → 均衡采样
3. Qwen3适配：修复chat template三处兼容问题
4. SFT warmup + IGPO RL 训练 + GRPO消融对照
5. 评估：Baseline 21 → GRPO 26.4 → IGPO 34.1

**Result（结果）**：
- IGPO 相对 Baseline **+62.4%**，相对 GRPO **+29.2%**
- 验证了 turn-level info gain 在多轮搜索任务上的有效性
- 全流程离线，可复现

### 3.2 遇到的困难及解决（经典问题）

**困难1：Qwen3 chat template 不兼容**
- 表象：31/32 rollout 格式错误，训练完全学不动
- 排查：对比Qwen2.5和Qwen3的chat template输出，发现Qwen3-Thinking自动注入`<think>`
- 解决：删除手动append + tool结果序列化 + SFT preformatted检测

**困难2：reward plateau 的根因诊断**
- 表象：reward在step 50后不涨了
- 排查：不只看指标，**抽查模型实际输出**，发现只会搜director
- 根因：80%训练数据是director类，模型过拟合单一pattern
- 解决：均衡采样

**困难3：BM25词法检索的局限**
- 表象：family类问题F1极低
- 根因：BM25无法语义匹配（"spouse"搜不到"married to"）
- 解决方案：提出双路并行检索（BM25+向量RRF），已写入简历作为改进亮点

**困难4：显存约束下的训练配置**
- 表象：4B+FSDP+vLLM 同卡显存不够
- 解决：gpu_mem_util=0.30、context限6400、max_turns限5、LoRA

### 3.3 为什么这么做 / 想解决什么问题

| 决策 | 想解决的问题 |
|------|------------|
| 本地BM25替代在线搜索 | 成本 + 可复现性 |
| BM25可回答性过滤 | 保证reward信号有效（答案能搜到才有F1） |
| SFT warmup | 基座没有工具调用能力，直接RL信号全为-2 |
| IGPO而非纯GRPO | 多轮搜索的信用分配问题 |
| 均衡采样 | 数据失衡导致的过拟合 |
| LoRA + 4B | 算力约束下的可行方案 |

**最终解决了吗**：
- ✅ 算法增益验证成功（IGPO > GRPO > Baseline）
- ✅ 全流程离线可复现
- ⚠️ 部分解决：数据失衡靠均衡采样缓解，但BM25词法局限未根治（需向量检索）

**为什么能解决**：
- IGPO有效是因为它**对症**——多轮搜索的本质难点是搜索策略，info gain直接奖励"搜到有用信息"
- 相对提升显著说明算法本身的价值，不依赖模型规模

**为什么部分解决不了**：
- BM25词法鸿沟是检索引擎本质局限，非算法能解决，需要换/加检索方式
- 测试集未过滤，部分题答案在索引外，F1理论上限<100%

### 3.4 如果重新做，怎么做得更好（经典问题）

**1. 检索层面**：
- 一开始就用**双路并行检索（BM25 + 向量 RRF）**，解决词法鸿沟，提升family类召回
- passage向量离线预算好（FAISS），在线只算query向量，速度可接受

**2. 数据层面**：
- 过滤时**模拟真实多跳检索**（分跳检索），而非用整句单query，避免"留假多跳删真多跳"
- 引入更多样数据集（HotpotQA、MuSiQue），不依赖单一2Wiki的类型分布
- 训练时就开**验证集监控**（test_freq），更早发现plateau和过拟合

**3. 算法/训练层面**：
- 更大上下文（32K）支持更多轮搜索（原版10轮）
- 更多rollouts（n=16）降低advantage噪声
- 尝试 curriculum learning（IGPO代码里已支持但未启用）

**4. 评估层面**：
- 加按类型分桶的评估（director/family/...分别看F1），定位短板
- 加训练集外的泛化测试集（HotpotQA等），验证迁移能力

**5. 工程层面**：
- checkpoint持久化到磁盘（之前放/dev/shm，断电即丢）
- 数据构建脚本入库（之前是交互式跑的，没commit）

---

## 附：高频追问快答

**Q: 为什么不直接用在线搜索？**
A: 成本（API按次收费）+ 可复现性（在线结果随时间变）+ 速度（网络延迟）。本地BM25确定性、免费、快。

**Q: IGPO 比 PPO 好在哪？**
A: PPO需要Critic网络估value（额外模型+训练不稳定）；IGPO/GRPO用组内rollout均值当baseline，省掉Critic。IGPO在GRPO上又加了turn-level信号解决多步信用分配。

**Q: KL散度一直涨怎么办？**
A: KL涨说明策略离ref模型越来越远，可能丢失通用能力或reward hacking。监控+早停+调大kl_coef。本项目KL到50+是风险点。

**Q: 为什么 γ=1.0 不折扣？**
A: 多轮搜索每轮获取的信息同等重要，不应因离终点远就打折。γ<1会让模型短视，只关注最后一两轮。

**Q: info gain 会不会被reward hacking？**
A: 有风险——模型可能生成"看起来让gt logprob升高"的搜索，但实际没用。靠F1外在奖励约束 + KL约束 + 真实搜索结果验证来缓解。

**Q: LoRA 和全参微调的取舍？**
A: LoRA省显存、快、可插拔；但上限低于全参。本项目算力受限选LoRA，工业部署也常用LoRA。研究冲SOTA用全参。
